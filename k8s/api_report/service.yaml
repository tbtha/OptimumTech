apiVersion: v1
kind: Service
metadata:
  name: api-report
  namespace: optimumtech
  labels:
    app: api-report
spec:
  selector:
    app: api-report
  ports:
    - port: 8084
      targetPort: 8084
  type: ClusterIP
