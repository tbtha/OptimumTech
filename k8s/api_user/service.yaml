apiVersion: v1
kind: Service
metadata:
  name: api-user
  namespace: optimumtech
  labels:
    app: api-user
spec:
  selector:
    app: api-user
  ports:
    - port: 8081
      targetPort: 8081
  type: ClusterIP
